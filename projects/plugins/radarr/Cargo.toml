[package]
name = "radarr"
version.workspace = true
edition.workspace = true

[lib]
name = "radarr"
path = "src/lib.rs"

# Radarr orca plugin. Thin tool surface over the shared `arr` base library
# (typed clients + common ops). Radarr speaks `/api/v3` with a `/movie`
# media model; app-specific media tools (radarr.add_movie, …) land later.

[dependencies]
arr = { path = "../arr" }
plugin-toolkit = { path = "../../plugin-toolkit" }

# `#[orca_tool]` / `endpoint_resource!` expansions reference these by path.
macro-runtime = { path = "../../macro-runtime" }
dispatch    = { path = "../../dispatch" }
contract    = { path = "../../contract", default-features = false }
derive      = { path = "../../derive" }
schemars    = { workspace = true }
inventory   = { workspace = true }
db          = { package = "db", path = "../../db" }
rusqlite    = { workspace = true }
clap        = { workspace = true }
async-trait = { workspace = true }

serde       = { workspace = true }
serde_json  = { workspace = true }
anyhow      = { workspace = true }

[dev-dependencies]
tokio    = { workspace = true, features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
