[package]
name = "prowlarr"
version.workspace = true
edition.workspace = true

[lib]
name = "prowlarr"
path = "src/lib.rs"

# Prowlarr orca plugin. Thin tool surface over the shared `arr` base library
# (typed clients + common ops). Prowlarr is the indexer-manager of the stack:
# it speaks `/api/v1` and is the only flavor with `/indexerstatus`. Indexer
# add + sync-to-apps tools land later.

[dependencies]
arr = { path = "../arr" }
plugin-toolkit = { path = "../../plugin-toolkit" }

# `#[orca_tool]` / `endpoint_resource!` expansions reference these by path.
macro-runtime = { path = "../../macro-runtime" }
dispatch    = { path = "../../dispatch" }
contract    = { path = "../../contract", default-features = false }
derive      = { path = "../../derive" }
schemars    = { workspace = true }
inventory   = { workspace = true }
db          = { package = "db", path = "../../db" }
rusqlite    = { workspace = true }
clap        = { workspace = true }
async-trait = { workspace = true }

serde       = { workspace = true }
serde_json  = { workspace = true }
anyhow      = { workspace = true }

[dev-dependencies]
tokio    = { workspace = true, features = ["macros", "rt-multi-thread"] }

[lints]
workspace = true
