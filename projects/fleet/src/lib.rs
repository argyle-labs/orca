//! Fleet domain — infra, meta. (pod/mesh moved to projects/pod/ in slice 4;
//! host* moved to `system` in slice 5; lifecycle dissolved into `system` in
//! slice B3.) The rest dissolves in slice 6.

pub mod infra;
pub mod meta;
