[package]
name    = "orca-nfs"
version = "0.1.0"
edition.workspace = true

[lib]
name = "orca_nfs"
path = "src/lib.rs"

[dependencies]
serde     = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
tokio     = { version = "1", features = ["process", "time", "rt"] }
